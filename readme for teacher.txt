You can choose to run the program in jar or in import in java IDE such as Eclipse.
The user manual is in doc files, you can read the user manual and use the program
The UML diagram, backlog and UI prototype is in "doc/design doc" file
If you run in eclipse, the main function is in control package's MainFunction.java. Run this class, then you can run the whole system.
We have prepare a VIP member number who has already have 10 virtual stamps. The member number is 697184. 
You can use this member number to have discount quickly. Or you can change the virtual number in member.csv which in the data folders.
